#include <iostream>
#include <queue>
#include <stack>
#define long long ll
using namespace std;
int rec(int x)
{
    if(x==0)
    {
        return 0;
    }
    return x+rec(x-1);
}
int main() {
    int x;cin>>x;
    cout<<rec(x);
    return 0;
}
